require "spec_helper"

RSpec.describe CloudbleedChecker do
  it "has a version number" do
    expect(CloudbleedChecker::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
